# 3D (Ring_FullyConnected_Switch) topology
topology: [ Ring, Switch ]

# 2 x 8 x 4 = 64 NPUs
npus_count: [ 4, 2 ]  # number of NPUs

# Bandwidth per each dimension
bandwidth: [ 25, 100.0, 50.0 ]  # GB/s

# Latency per each dimension
latency: [ 50.0, 500.0, 2000.0 ]  # ns
